/*************************************************************************
 * libjson-rpc-cpp
 *************************************************************************/

/*
 * Role in DigiAsset for Windows:
 * Part of the bundled libjson-rpc-cpp library. A thin indirection header that
 * pulls in the JsonCpp public API (Json::Value, parser, writer) so the rest of
 * the JSON-RPC code has a single, stable include point for JSON types.
 */

#ifndef JSONRPC_CPP_JSONPARSER_H_
#define JSONRPC_CPP_JSONPARSER_H_

#include <jsoncpp/json/json.h>

#endif // JSONRPC_CPP_JSONPARSER_H_
